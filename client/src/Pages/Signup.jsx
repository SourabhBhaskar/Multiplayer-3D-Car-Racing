import React from 'react';

function Signup() {
  const handleSubmit = (e) => {
    e.preventDefault();
  }

  return (
    <form onSubmit={handleSubmit}>
      
    </form>
  )
}

export default Signup;